# MICROSERVICES ARCHITECTURE DTO

comando para poblar la base de datos de mysql en el docker compose;

```bash
docker exec -i mysql_db mysql -uroot -pRootPass123! customercataloguedb<Customer.sql
```
